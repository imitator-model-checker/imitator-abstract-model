# IMITATOR abstract model

This Java projects contains the necessary classes and models to represent an abstract IMITATOR model, and its parsing methods to a concrete IMITATOR model.

⚠️ Only a quite reduced subset of the syntax is considered so far.

⚠️ The printing was not much tested so far.
